function removeDuplicates(str1,str2) {
   const sortedStr1=str1.split('').sort().join();
   const sortedStr2=str2.split('').sort().join();
   console.log(sortedStr1,"radhe radhe",sortedStr2);

   const removeSameChar1 = sortedStr1.
   const removeSameChar2 = sortedStr2.
}
console.log(removeDuplicates("prithwi","dipika"));